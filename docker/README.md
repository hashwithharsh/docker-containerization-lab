# Docker files

The project Dockerfiles in this directory are the versions I worked on while learning the application images. I kept the application source in its original service directories and kept my Docker changes here so the difference is easy to see.

- `vote/` - Python image notes and Dockerfile
- `result/` - Node.js image Dockerfile
- `worker/` - .NET multi-stage Dockerfile
- `seed-data.Dockerfile` - seed job image copied from the sample for reference
